
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user of the NexBot application, which can be either a registered user or a temporary guest.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "displayName": {
          "type": "string",
          "description": "A user-friendly name chosen by or assigned to the user profile."
        },
        "isGuest": {
          "type": "boolean",
          "description": "Indicates whether this user profile is for a temporary guest session (true) or a persistent user (false)."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user profile was created.",
          "format": "date-time"
        },
        "lastActiveAt": {
          "type": "string",
          "description": "Timestamp of the last known activity for this user profile.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "isGuest",
        "createdAt",
        "lastActiveAt"
      ]
    },
    "ChatSession": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ChatSession",
      "type": "object",
      "description": "Represents a single, continuous conversation thread between a user and the AI.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ChatSession entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to UserProfile. (Relationship: UserProfile 1:N ChatSession)"
        },
        "title": {
          "type": "string",
          "description": "A user-editable title or a generated summary for the chat session, making it easier to identify in history."
        },
        "isPinned": {
          "type": "boolean",
          "description": "Flag to indicate if the chat session is pinned to the top of the list."
        },
        "isArchived": {
          "type": "boolean",
          "description": "Flag to indicate if the chat session is moved to the archives and hidden from the main list."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the chat session was first initiated.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the chat session was last updated, typically when a new message was added.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "title",
        "createdAt",
        "updatedAt"
      ]
    },
    "ChatMessage": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ChatMessage",
      "type": "object",
      "description": "Represents an individual message within a ChatSession, sent by either the user or the AI.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ChatMessage entity."
        },
        "chatSessionId": {
          "type": "string",
          "description": "Reference to ChatSession. (Relationship: ChatSession 1:N ChatMessage)"
        },
        "senderType": {
          "type": "string",
          "description": "Indicates who sent the message, e.g., 'user' or 'ai'.",
          "enum": [
            "user",
            "ai"
          ]
        },
        "content": {
          "type": "string",
          "description": "The textual content of the message."
        },
        "timestamp": {
          "type": "string",
          "description": "The exact time the message was sent or received within the chat session, used for ordering.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "chatSessionId",
        "senderType",
        "content",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ],
    "branding": {
      "projectName": "NexBot",
      "supportEmail": "support@nexbot.com"
    }
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores individual user profiles. Each document ID corresponds to the Firebase UID.",
          "params": [
            {
              "name": "userId",
              "description": "The unique Firebase Authentication User ID."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/chatSessions/{chatSessionId}",
        "definition": {
          "entityName": "ChatSession",
          "schema": {
            "$ref": "#/backend/entities/ChatSession"
          },
          "description": "Stores individual chat sessions for a specific user.",
          "params": [
            {
              "name": "userId",
              "description": "The owner's Firebase UID."
            },
            {
              "name": "chatSessionId",
              "description": "The unique session ID."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/chatSessions/{chatSessionId}/chatMessages/{messageId}",
        "definition": {
          "entityName": "ChatMessage",
          "schema": {
            "$ref": "#/backend/entities/ChatMessage"
          },
          "description": "Stores messages within a specific chat session.",
          "params": [
            {
              "name": "userId",
              "description": "The owner's Firebase UID."
            },
            {
              "name": "chatSessionId",
              "description": "The parent session ID."
            },
            {
              "name": "messageId",
              "description": "The unique message ID."
            }
          ]
        }
      }
    ]
  }
}
